<h1>Goal Tiers</h1>

<table>
<% @tiers.each do |tier| %>
  <tr>
    <td><%= link_to tier.name, tier %></td>
    <td><%= tier.note %></td>
    <td><%= tier.member.first_name %></td>
    <td><%= link_to pluralize(tier.endeavors.count, 'goal'), tier_endeavors_path(tier) %>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tier', new_tier_path %>
