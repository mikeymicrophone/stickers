<%= form_for(@sub_club) do |f| %>
  <% if @sub_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_club.errors.count, "error") %> prohibited this sub_club from being saved:</h2>

      <ul>
      <% @sub_club.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :size => '40x4' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
